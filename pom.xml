<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.praticando</groupId>
    <artifactId>praticando-apiurl</artifactId>
    <version>1.0</version>
    <packaging>war</packaging>
    <name>Praticando API con Servlets + JSP</name>

    <properties>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <tomcat.version>10.1.33</tomcat.version>
    </properties>

    <dependencies>

        <!-- ══ Tomcat 10 Embebido: servidor web completo en el classpath ══ -->
        <!-- Incluye jakarta.servlet-api automáticamente -->
        <dependency>
            <groupId>org.apache.tomcat.embed</groupId>
            <artifactId>tomcat-embed-core</artifactId>
            <version>${tomcat.version}</version>
        </dependency>

        <!-- ══ Jasper: motor que compila y ejecuta los archivos JSP ══ -->
        <dependency>
            <groupId>org.apache.tomcat.embed</groupId>
            <artifactId>tomcat-embed-jasper</artifactId>
            <version>${tomcat.version}</version>
        </dependency>

        <!-- ══ org.json: parseo de JSON (reemplaza el JAR local) ══ -->
        <dependency>
            <groupId>org.json</groupId>
            <artifactId>json</artifactId>
            <version>20240303</version>
        </dependency>

    </dependencies>

    <build>
        <!-- Apuntar al directorio src/ existente (en lugar de src/main/java) -->
        <sourceDirectory>src</sourceDirectory>

        <plugins>

            <!-- ══ Compilador ══ -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>17</release>
                </configuration>
            </plugin>

            <!-- ══ WAR plugin: usa web/ en lugar de src/main/webapp ══ -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.4.0</version>
                <configuration>
                    <warSourceDirectory>web</warSourceDirectory>
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                </configuration>
            </plugin>

            <!-- ══ exec-maven-plugin: ejecuta EmbeddedServer.main() ══ -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.3.0</version>
                <configuration>
                    <mainClass>EmbeddedServer</mainClass>
                </configuration>
            </plugin>

        </plugins>
    </build>

</project>
